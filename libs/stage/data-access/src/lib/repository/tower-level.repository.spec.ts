import { TowerLevelRepository } from './tower-level.repository';

describe('TowerLevelRepository', () => {
  it('should create an instance', () => {
    expect(new TowerLevelRepository()).toBeTruthy();
  });
});
